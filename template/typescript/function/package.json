{
  "name": "openfaas-function",
  "version": "1.0.0",
  "description": "OpenFaaS Function",
  "main": "handler.js",
  "es6": true,
  "scripts": {
    "build": "tsc --project .",
    "clean": "tsc --build --clean",
    "test": "mocha --experimental-modules",
    "test:local": "npm run build && mocha --experimental-modules && npm run clean"
  },
  "keywords": [],
  "author": "OpenFaaS Ltd",
  "license": "MIT",
  "dependencies": {
    "@inverti-facil/core": "0.0.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.2.2",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.13.4"
  }
}
